 
  // import {Store} from './Store';
  // import {updateViewState} from "../app"
 

  // Store.subscribe(handler);
  
  
  // function handler(){
  //   let state=Store.getState();
  //   if (!$.isEmptyObject(state) && !$.isEmptyObject(state.menuReducer) && state.menuReducer.actionType === "CurrentUserInfoUpdate")
  //   {
  //     //updateViewState(state.currentView.Name)
  //   }
  //   else if (!$.isEmptyObject(state) && !$.isEmptyObject(state.menuReducer) && state.menuReducer.actionType === "CurrentViewUpdate")
  //   {
  //     updateViewState(state.menuReducer.currentView.Name)
  //   }
   
 
  // }


